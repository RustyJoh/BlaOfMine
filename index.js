document.getElementsByClassName("button-background")[0].addEventListener("change", backgroundImage, false);

function backgroundImage (e) {
  document.getElementsByClassName('button-background').input[0].files
}